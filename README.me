# Proyecto

## Instalación

1. Clone el repositorio o descargue el ZIP.
2. Ingrese a la carpeta del proyecto.
3. Ejecute `docker-compose up --build` desde una terminal Linux (PC o servidor).
4. Ingrese a [http://localhost:3000](http://localhost:3000) en su navegador.

